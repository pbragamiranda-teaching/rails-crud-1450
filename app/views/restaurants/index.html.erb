<h1>Check all the restaurants</h1>

<%= link_to "Create new Restaurant", new_restaurant_path %>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to restaurant.name, restaurant_path(restaurant) %>
      (<%= restaurant.rating %> ⭐) - <%= restaurant.address %>
    </li>
  <% end %>
</ul>
